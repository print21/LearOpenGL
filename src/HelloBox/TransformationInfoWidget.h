
#pragma once

#include <QWidget>

class TransformationInfoWidget : public QWidget
{
    Q_OBJECT;
public:
    explicit TransformationInfoWidget(QWidget * parent = nullptr);
    ~TransformationInfoWidget();
};

